package com.dhouib.foobarquix.service;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;


/*class NumberParserServiceImplTest {


    private final NumberParserService transformer = new NumberParserServiceImpl();

    @Test
    void testSingleNumber() {
        assertEquals("1", transformer.parse(1));
        assertEquals("FOOFOO", transformer.parse(3));
        assertEquals("BARBAR", transformer.parse(5));
        assertEquals("QUIX", transformer.parse(7));
    }

    @Test
    void testComplexCases() {
        assertEquals("FOOBAR", transformer.parse(51));
        assertEquals("BARFOO", transformer.parse(53));
        assertEquals("FOOFOOFOO", transformer.parse(33));
        assertEquals("FOOBARBAR", transformer.parse(15));
    }
}
*/
